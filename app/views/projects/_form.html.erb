<%= form_with(model: @project, local: true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class=:"field">
    <%= f.label :power %>
    <%= f.text_field :power %>
  </div>
  <%= f.submit nil, class: "ui blue button" %>
<% end %>